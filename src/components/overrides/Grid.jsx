import styled from 'styled-components';
import { Grid as RfgGrid, Row as RfgRow, Col as RfgCol } from 'react-flexbox-grid';

export const Grid = styled(RfgGrid)`
  padding: 0;
`;

export const Row = styled(RfgRow)`
  margin: 0;
  padding: 0;
`;

export const Col = styled(RfgCol)`
  padding: 0;
`;
